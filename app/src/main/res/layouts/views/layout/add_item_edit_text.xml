<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:gravity="center_vertical">
    <ImageView
        android:id="@+id/aiet_icon"
        android:layout_width="@dimen/big_icon_height"
        android:layout_height="@dimen/big_icon_height"
        android:src="@drawable/ic_symb_plus_small"
        app:tint="@color/grey"
        android:layout_margin="10dp"/>
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">
        <TextView
            android:id="@+id/aiet_edit_warning"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="warning: "
            android:textSize="@dimen/small_textsize"
            android:textColor="@color/warning_text"
            android:textStyle="bold"
            android:visibility="gone"/>
        <EditText
            android:id="@+id/aiet_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/yellow_bar_text_newevent"
            android:inputType="textMultiLine"
            android:theme="@style/EditTextTheme"
            android:textSize="@dimen/page_item_textsize"
            android:textColor="@color/black"
            android:background="@null"/>
    </LinearLayout>
</LinearLayout>